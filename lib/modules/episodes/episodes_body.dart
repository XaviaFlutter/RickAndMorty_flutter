import 'package:flutter/material.dart';

class EpisodesBody extends StatelessWidget {
  const EpisodesBody({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Эпизоды'),
    );
  }
}
